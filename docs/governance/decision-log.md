# Product and architecture decision log (S01-10)

Add a new entry for every important decision. Keep entries even if a later decision reverses them — the history matters.

---

**2026-09-24 — Stage 14 wrap-up: remaining rate-limit gaps fixed, five write-ups closed**
Owner: Claude Code. Reason: continuing Stage 14 after the CI/LCP detour above. Two more real, live-verified fixes: (1) S14-05 — `mfa_challenge`, `report`, and `invitation` had no rate limiting at all (the confirmed gaps from the original 4-agent audit); extended the existing table-backed `rateLimit.ts` mechanism (widened `auth_rate_limit_attempts`' check constraint via migration 0089, verified live against the test project) rather than building a new one. `mfa_challenge` (5/15min) closes the most severe of the three — previously a staff account with a stolen password but no authenticator device could brute-force a 6-digit TOTP code indefinitely. (2) S14-02 follow-up — investigating the audit's account-enumeration flag on signup found the literal error-message leak doesn't actually trigger (Supabase itself suppresses the error for an already-registered email, verified live), but surfaced a real bug instead: the code didn't check for that case, so it wrote a phone-verification-reminder notification into an existing, unrelated user's account on every such attempt, unauthenticated. Fixed by checking Supabase's own `identities: []` signal.

Closed the five remaining documentation/synthesis items with dedicated write-ups rather than one combined document, matching `stage-14-backup-restore-evidence.md`'s established one-doc-per-line-item precedent: `stage-14-asvs-assessment.md` (S14-06), `stage-14-privacy-data-flow-review.md` (S14-12), `stage-14-self-red-team.md` (S14-14), `stage-14-architecture-review.md` (S14-16), `stage-14-deployment-review.md` (S14-17) — each cites the specific audit finding or code location behind every claim rather than asserting from memory (the deployment review's staff-console hosting claim was corrected mid-write after checking `_headers`/`render.yaml` directly instead of guessing).

**S14-15's exact tracker wording is still unknown to me** — it wasn't in this session's tracked pending-work list and I have no Excel tracker access in this environment to check it directly. Flagging rather than silently skipping or fabricating content for it. The Stage 14 Excel tracker update (all 17 rows) also remains outstanding — the surgical update pipeline used for Stages 12/13 needs to be rebuilt in this session's scratchpad before that can happen.

**2026-09-24 — Founder-confirmed Lighthouse CI LCP budget: 3000ms, not the default 2500ms**
Owner: Founder. Reason: the founder pasted a CI failure on `/login`/`/signup`'s LCP assertion and asked to fix it. Investigation found and fixed two genuine bugs, not the budget itself: (1) `src/instrumentation-client.ts` statically imported the full `@sentry/nextjs` SDK unconditionally, even with no DSN configured — a ~120KB chunk taking over 1.3s to parse under Lighthouse's mobile CPU throttling, now dynamically imported and gated on the DSN existing; (2) `src/proxy.ts` (the auth-refresh middleware, runs on every request) called `supabase.auth.getUser()` with no timeout — in CI, against a fake placeholder URL, this was blocking every single page response; confirmed directly via real (non-simulated) `curl` timing that this dropped TTFB from ~500ms to ~15ms. Also disabled unnecessary Next.js Link prefetching on 4 auth-page links that were firing real server-side RSC renders competing for CPU with the actual request on CI's shared runner. These closed most of the original gap (~360-500ms over budget down to ~200-350ms). What remained, confirmed via a diagnostic script added to `ci.yml` (`platform/scripts/lh-diagnose.mjs`) that prints real CI Lighthouse data instead of relying on an unreliable local Windows reproduction, was normal LCP run-to-run variance on a strict threshold — main-thread cost and Render Delay had converged to nearly identical values across all three tested pages, including the one that already passed. Impact: `.lighthouserc.json`'s `largest-contentful-paint` assertion raised from 2500ms to 3000ms — the industry-standard "good" default was never a founder-approved number to begin with (flagged as such in `ci.yml`'s own comment since Stage 12), and 3000ms is what this app's real, now-optimized auth pages reliably achieve.

**2026-09-19 — Stage 14 (security and senior engineering review): four parallel audits, three real vulnerabilities found and fixed live**
Owner: Claude Code. Reason: the founder asked to move to Stage 14 after Stage 13. Same distinction as every prior stage's role gap: S14's Security-Reviewer-owned items are technical code reviews I can genuinely perform (full code access, full understanding) — I just can't provide the independent certification an external reviewer or pentester would. Built `docs/governance/threat-model.md` (new, S14-01) first — assets/actors/trust-boundaries/abuse-case framing — then launched four parallel background audit agents against it, each with detailed file:line-citation requirements and an explicit "report only, no fixes" instruction: (A) auth/session/MFA/rate-limiting, (B) RLS and multi-tenant isolation (86 migrations, table-by-table), (C) input validation/injection/uploads/authorization-on-the-server, (D) dependencies/secrets/audit-log coverage.

**Three real vulnerabilities found and fixed, each verified live before committing (not just code-reviewed):**
1. **MFA could be bypassed entirely by calling `backend/api` directly** (S14-02, commit `dbe5e4a`). S04-08 requires every staff role to complete TOTP MFA, enforced via `requireStaffMfa()` — but only in the Next.js app. `backend/api/src/middleware/auth.js` never checked the JWT's `aal` claim at all, so a staff account that completed password login but never verified MFA still held a fully valid token every staff route accepted. Fixed by decoding the already-verified token's own `aal` claim and rejecting non-aal2 staff sessions with 403. Verified live against the test Supabase project: created a real staff account, confirmed a real aal1 (password-only) session token is now rejected, then did an actual TOTP enrollment + verification and confirmed the resulting real aal2 token still passes through correctly — not a fabricated-claim test.
2. **A reviewer/matcher account could issue a financial refund** (S14-04, commit `9536465`). `POST /api/disputes/:id/refund` only had the router-level `requireStaff` gate (any of reviewer/matcher/finance/admin), unlike every other money-moving route in `finance.js` which requires `requireFinanceStaff`. Gated the refund route specifically to finance/admin.
3. **Staff could silently tamper with fraud/scam flags outside the audit trail** (S14-11, commit `dfa759e`). `risk_flags` (0083) used a single `for all` RLS policy granting staff direct INSERT/UPDATE/DELETE, unlike every other audit-adjacent table (`audit_events`, `verification_events`, `engagement_events`, `assisted_field_changes`), which restrict direct writes to the service-role-only backend/api route. Restricted the policy to SELECT-only; the existing audited route (already using the service-role client) is unaffected.

**Also fixed:** two storage buckets (`org-logos`, `org-documents`) never got the server-side size/MIME enforcement every sibling bucket received in 0055/0069 — closed with `0087_org_bucket_upload_limits.sql`, matching each bucket's own upload component's client-side claims exactly (S14-09, commit `1298390`). `backend/api`'s 3 moderate qs/body-parser vulnerabilities fixed via `npm audit fix`, 0 remaining (S14-07, commit `0afc33e`).

**Found and explicitly left open, for the founder's attention rather than a unilateral call:** the `@lhci/cli` devDependency vulnerability decision from Stage 12 (still unresolved — agent D's review this round added real detail: a plain, current `lighthouse` install would avoid nearly all 10 vulnerabilities, but at the cost of rebuilding `lhci autorun`'s multi-run/budget-assertion logic the CI workflow currently depends on); a local-only, git-ignored `platform/.env.e2e.local` file holding real test-project credentials unencrypted on disk (never committed, but a live credential worth a rotation decision); account-enumeration via the signup error message, and missing rate limits on invitations/reports/MFA-challenge verification (all confirmed gaps, not yet fixed); `talent_profiles_select`'s RLS policy staleness relative to its sibling policies (functional gap, not a leak — needs a live test before deciding whether it's a real bug); a restore-rehearsal re-run is still needed before S14-13 can be signed off with current-schema confidence (I have no GitHub CLI access in this environment to trigger it — a founder-side click from the Actions tab).

Full detail: `docs/governance/threat-model.md`, `docs/governance/schema-and-ownership-map.md`'s new "Known RLS drift" section, `docs/governance/stage-14-backup-restore-evidence.md`.

**2026-09-19 — Stage 13 (legal, compliance and policy readiness): built what's actually buildable, flagged the rest**
Owner: Claude Code. Reason: the founder asked to continue to Stage 13 after Stage 12. Full detail in commit `a29c5e3`. Audited first (a dedicated background agent, not assumed) what already exists: `privacy.html`, `terms.html`, `community-standards.html`, and `cancellation-refunds.html` all already exist, are substantive, show a visible effective date/version, and — checked directly against `featureFlags.ts` — the refund policy's "payments are simulated, not real money" claim is factually accurate to the current code. A full claims audit (S13-13, "the platform does not promise outcomes it can't prove") found nothing to fix — every guarantee/promise-adjacent phrase already found in the copy is a deliberate disclaimer, not an overclaim.

**Ten of the tracker's fourteen Stage 13 items are owned by Legal Counsel** (S13-01/02/03/04/05/06/07/10/13/14 per column G) — a role this project doesn't have yet, the same gap already on record ("legal counsel... roles remain unfilled and genuinely blocked, since the playbook is explicit those cannot be substituted"). None of that legal work was attempted or fabricated. Where a Legal-Counsel-owned item has a preparable draft, one was built for their eventual review rather than left blank: `docs/governance/data-retention-policy.md` expanded into a real 5-category schedule (user records/applications/files/logs/backups — backups genuinely differ, a real 30-day technical limit, not a policy choice), and a new `docs/governance/safeguarding-procedure.md` proposes who receives a safeguarding report (every admin account, the safest default without inventing a named person, same reasoning as S10-10) and a response approach — both explicitly marked draft, not decided.

**Built for real** (the items actually owned by Engineer A/Founder): S13-09 — `staff/people.html` now shows each account's policy-consent version/date/source, closing a real gap where that evidence, written since migration 0062, had no way to be read back at all; verified live against the test project. S13-11 — new `docs/governance/third-party-vendors.md` consolidates every integrated vendor (10 of them) with what data each touches, which didn't exist as one document before. S13-12 — found and fixed a real bug of my own making: the transactional-email support address defaulted to `support@adorworks.org`, a domain nobody ever confirmed AdorWorks controls, instead of the site's real `info@adorworks.net` (my mistake from the Stage 11 session); added the platform app's first-ever visible support contact (previously zero anywhere in-app); improved the report-button's post-submission message to at least link to that contact, without inventing a response-time promise. Also caught, while in that file, that `report-button.tsx`'s submit button was in Stage 12's own 11-file contrast-fix list but never actually got fixed — corrected now.

Tracker updated 2026-09-19: 14 rows, judged against each one's literal acceptance criteria — S13-08/S13-09/S13-11 landed Complete (the actual mechanism/document work is done), S13-04/05/06/07/10/12/13/14 landed Partial (real prep done, formal Legal-Counsel-or-founder sign-off still pending — see the distinctions above), S13-01/02/03 stayed Not Started (genuinely nothing to do without counsel).

**2026-09-19 — Stage 12 (accessibility, responsive design, performance) closed out**
Owner: Claude Code. Reason: the founder asked to finish Stage 12 before moving to Stage 13, continuing from batch 1 (keyboard nav/focus/aria-live/automated testing, logged separately above). This closing batch covers S12-12 (Core Web Vitals), S12-13 (low-data experience), and S12-15 (interface language consistency) — full detail in commit `65d9b78`. Summary: built a real, enforced Lighthouse CI performance budget (industry-standard "good" CWV thresholds, mobile-throttled to match the pilot's connection profile) covering the three pages testable without a login flow; verified it live, not just wired up, and in doing so found that local machine contention (a large number of stray Chrome/Node processes from this session's own heavy e2e testing) was skewing measurements by roughly 16 performance points — a real methodological catch, not assumed. S12-15 got a broader same-day sweep of action-button labels across the app; found no genuine inconsistency (apparent near-duplicates turned out correctly distinct in context), confirming rather than contradicting the original audit's finding.

**Real, documented gap left open, not silently dropped**: Lighthouse CI only covers unauthenticated pages (/, /login, /signup) — every other priority page (dashboard, opportunities, passport, contracts, etc.) requires a login and isn't covered by this budget yet. Closing that needs a Puppeteer login script feeding an authenticated session into Lighthouse CI, which Lighthouse CI supports but wasn't built in this pass. Flagging as the natural next step whenever performance work on Stage 12 continues.

**Stage 12 final status, all 15 items addressed**: S12-01 through S12-05, S12-07, S12-08, S12-12, S12-14 built and verified live this session; S12-06/S12-09/S12-10/S12-11 confirmed already in good shape (the new axe-core suite now gives S12-06/S12-09 ongoing automated regression coverage); S12-13 partially covered (the three Lighthouse-tested pages are mobile-throttled, but authenticated discovery/apply journeys aren't yet); S12-15 confirmed with no fix needed. Tracker updated 2026-09-19 (see entry below) — S12-12, S12-13 and S12-14 landed as Partial rather than Complete per the tracker's literal acceptance criteria (narrower page/journey/browser coverage than the criteria's full wording implies); see that entry for the distinction.

**2026-09-19 — Stage 12 (accessibility, responsive design, performance) batch 1: keyboard nav, focus states, aria-live, automated testing**
Owner: Claude Code. Reason: continuing the founder's "work on the next stage" request after Stage 11. Reused the prior session's three-agent audit rather than re-auditing from scratch (still fresh), then built the tractable fixes and, critically, two new automated e2e suites (`platform/e2e/accessibility.spec.ts` using `@axe-core/playwright`, and an expanded `mobile-responsive.spec.ts` now covering mobile/tablet/desktop across 10 priority journeys) so future regressions get caught automatically rather than needing another manual audit. Running those suites live — not just adding them — surfaced real bugs no code review would have caught: a dashboard card's description text failed WCAG AA contrast (2.04:1) against its own teal background; 11 buttons/badges across the app used a bright coral background with white text at 2.81:1 (fixed by swapping to the already-existing, already-computed `coral-ink` variant — same fix pattern the codebase already used for coral-as-text, just never applied to coral-as-background); 10 file inputs had no accessible name at all; the organisation dashboard's header overflowed horizontally at 360px width (no `flex-wrap` on a two-column row). Also fixed: `mode-switcher.tsx`'s dropdown had no keyboard-accessible way to close (Escape now works); ~30 form components' error/status messages had no `role="alert"`/`role="status"`, so screen readers never heard about validation failures or confirmations. Building the new specs also exposed a second-order bug in the test suite itself: calling the login form fresh for every test against the same seeded user tripped the real 5-attempts/15-minutes rate limit once each suite grew past a few pages — fixed properly with Playwright storage-state reuse (one real login per user per file) rather than working around the limit. New `.github/workflows/e2e.yml` runs the full suite (now including firefox/webkit, scoped to the priority-journey spec to keep runtime bounded) — manually triggered only, not on every push, since this suite mutates the same Supabase project that backs staging (2026-09-12 decision-log entry) and automatic-on-every-push risks colliding with a staging review session. Needs 5 repository secrets for the test project, documented in the workflow's own comment, never requested in chat.

**Two pre-existing bugs found, confirmed real, deliberately not fixed here** — out of scope for accessibility/responsive/performance work, confirmed pre-existing by running the failing tests against the last commit before any Stage 12 change (not assumed): (1) `e2e/opportunity-lifecycle.spec.ts`'s "non-staff org member cannot submit an incomplete opportunity for review" test fails with a real Postgres error, `function reject_unless_staff(boolean, unknown) does not exist` — a function signature mismatch blocking a specific RLS guard path on the test project, worth checking against production too. (2) The same file's opportunity-discovery test finds a closed opportunity still appears in search results after being closed and the page reloaded — a stale-result/cache-invalidation gap. Both need a proper look under whichever stage covers opportunity lifecycle/discovery, not bundled into this one.

Tracker updated 2026-09-19 (see the closing entry above) alongside the S12-12/S12-13/S12-15 follow-up batch. S12-06/S12-09/S12-10/S12-11 were already "largely complete" per the prior audit and the new axe suite provides ongoing automated coverage for the form-labeling/image-alt half of that going forward.

**2026-09-19 — Excel tracker updated for Stage 10 and Stage 11 (27 rows)**
Owner: Claude Code. Reason: the tracker still showed every Stage 10/11 row as "Not Started" despite both stages being built out in the two entries below — closing that gap. Updated via the established surgical raw-XML pipeline (`update-tracker.mjs` + `swap-entry.mjs`): backed up first (`AdorWorks_MVP_Development_Tracker.backup-2026-09-19-before-stage10-11.xlsx`), verified via a full-sheet cell diff that all 138 changed cells fall only within the 27 target rows (0 unexpected diffs elsewhere), and re-read every written cell back to confirm it matches what was intended before overwriting the live file. Judged each of the 27 items against its literal acceptance-criteria text (column F), not against my own summary of the work — this changed two verdicts from what the work alone might suggest: S10-04 (organisation verification) stayed **Partial**, not Complete, because the override endpoint still lets staff bypass evidence requirements by design (now accountable — a mandatory, audited reason — but the bypass itself remains, so "without bypassing evidence requirements" isn't literally met); S11-04 (events on both channels) stayed **Partial** for the same kind of reason — the platform side is fully wired, but backend/api's verification-decision notifications have no email pair since that service has no email capability at all, and "verification" is one of S11-02's named required categories. Also found and fixed one real, cheap gap while checking criteria literally: S11-03 requires "brand, action, support and safety information" in email templates, and the shared template had the first three but no safety/anti-phishing note — added one (`emailTemplate.ts`, commit `357720d`) before marking S11-03 Complete. Full per-item evidence and notes are in the tracker itself, not duplicated here.

**2026-09-19 — Stage 11 (notifications and communication) built out and closed**
Owner: Claude Code. Reason: continuing the founder's "start the other remaining stages" request, following Stage 10. Audited via three parallel background agents against the tracker's exact acceptance criteria, then built the real gaps across three commits. S11-05 (duplicate-notification prevention): `notifications.dedupe_key` defaults to a fresh random value per row and a unique index on `(user_id, type, dedupe_key)` lets a caller opt a specific notification into real database-level dedup — applied to ~15 call sites where the (user, type) pairing is a genuine one-way transition, deliberately left off three types where a second occurrence is legitimate (opportunity decisions after a resubmit cycle, a contract's second dispute, every chat message). S11-06 (delivery failure visibility + retry): both `notifyUser()` and `sendEmailSafely()` now record a failed send to `audit_events` — reusing the existing staff activity log (`staff/people.html`) rather than building a second table and page — and `sendEmail()` retries once with a short backoff before giving up. S11-03/S11-10: a shared `emailTemplate.ts` (header/footer/brand/support contact/unsubscribe link, with an `escapeHtml()` every call site now uses) replaced three ad hoc inline-HTML email builds, fixing a real HTML-injection gap (an employer's free-text rejection reason and an opportunity's title were both being interpolated unescaped). S11-07/S11-08 (preferences, unsubscribe): `profiles.email_notifications_enabled` (default true) plus a signed, session-free unsubscribe token (`lib/unsubscribeToken.ts`) so the emailed link works for someone not signed in — the actual unsubscribe write is a POST-triggered server action, not a bare page-load GET, since email-client link prescanning fetches every link automatically. S11-04 (events on both channels): every `notifyUser()` call site in `platform/src` that lacked an email pair now has one — milestones, offers, invitations, messages, reviews, timesheets, team changes, service requests — with two documented exceptions: the phone-verification reminder (fires the same moment as Supabase's own signup-confirmation email, so a second simultaneous email would be noise) and organisation-verification decisions (backend/api's Express app has no email capability at all; building a second, parallel sender there for one low-frequency event isn't proportionate — a shared email service both apps call would be the real fix). S11-02 (event inventory): corrected a false "dead type" finding from the initial audit (`evidence_reviewed`/`introduction_video_reviewed` are fired from backend/api's talent.js, a directory the audit's grep scope missed), then closed the real gaps — timesheet review, contract review, organisation verification decisions, a password-changed security notice (previously zero notification of any kind for a changed password — a real security gap), and organisation team role-change/removal now all notify, where none did before. Left out on purpose: scorecards (staff-internal, not meant for the candidate), the staff-to-staff maker-checker role-promotion workflow (low-frequency, staff already see it in the console they're using), and phone-verification-completed (redundant with the UI's own verified badge). S11-01, S11-09, S11-11 were already complete going in. S11-12 (timestamp/timezone): added a documented `PILOT_TIMEZONE` constant (`Africa/Juba` — a fixed UTC+3 offset, no DST, so this is a correctness fix not a cosmetic one) and applied it via shared `formatDate()`/`formatDateTime()` helpers across all 11 real date-display call sites. Every batch verified live against the test Supabase project (dedup upsert behavior, the email-preference gating query, and a real `PATCH /api/organisations/:id/verify` call producing the expected notification row) before commit; platform lint/build/typecheck and backend/api lint/tests stayed clean throughout. Tracker updated 2026-09-19 (see entry above) — S11-04 and S11-02 landed as Partial/Complete respectively per the tracker's literal acceptance criteria, not a blanket "done"; see that entry for the distinction.

**2026-09-19 — Stage 10 (staff operations, trust and support) built out**
Owner: Claude Code. Reason: the founder asked to continue with the remaining tracker stages after Stage 6-9 was closed out. Audited via three parallel background agents against the tracker's exact acceptance criteria, then built the real gaps across two commits. Fixed: talent verification review required no rejection reason and wrote no audit trail for evidence/video decisions (S10-03); the organisation-verification override endpoint let staff mark an org 'verified' with zero evidence on file and no explanation — kept the endpoint (a legitimate off-platform-verification path) but made the reason mandatory and flagged evidence-free decisions in the audit metadata for spot-checking (S10-04); reports gained severity, staff assignment, and a mandatory resolution reason, none of which existed before (S10-06); account suspension/reinstatement didn't exist at all despite `profiles.status` already having the enum — built both endpoints, including rotating the suspended user's password to end existing sessions within the hour (S10-08); a new staff-only `risk_flags` table gives staff a proactive "something looks off" signal, distinct from the reactive user-submitted `reports` table (S10-11); safeguarding/PSEA reports previously had no distinct category and were visible to every staff role identically — added a `safeguarding` reason and restricted it to admin-only at the RLS layer (not just the UI), documented as a structural fix since who the ultimately-responsible named contact should be is a founder policy call not yet made (S10-10); several staff actions (talent-service publish/reject/pause, assisted-onboarding account provisioning) had no audit trail at all despite being sensitive (S10-14). Deliberately left unbuilt: reviewer/matcher role separation (S10-01) and a general appeals workflow (S10-09) both lack a clear product spec to build against; the opportunity-review-queue reconciliation (S10-05) and staff-dashboard reconciliation (S10-02) are larger, already-flagged cross-implementation drifts; SLA targets (S10-12) and export limiting (S10-15, no export feature exists yet to limit) have no code action to take. Also found and fixed a second occurrence of the `_schema_migrations` tracking-table drift (see the 2026-09-12 entry below) on the test project. Every fix verified live against the test Supabase project before commit; backend/api lint and tests stayed clean throughout. Tracker updated 2026-09-19 (see entry above) — S10-04 and S10-10 landed as Partial, not Complete, per the tracker's literal acceptance criteria; see that entry for why.

**2026-09-16 — Password reset and signup confirmation were completely broken for every user; fixed**
Owner: Claude Code, reported by the founder ("why is it so hard for a user to reset their password"), clarified as "a user can receive a password reset link or magic link but cannot reset their password." Root cause, confirmed live against the real Supabase project rather than guessed: `resetPasswordForEmail` and signup confirmation both deliver the session as a URL fragment (`#access_token=...&refresh_token=...`), not a `?code=` query param — but `/auth/callback` was a server Route Handler, which can never see a URL fragment at all (browsers strip it before the request even reaches the server). `code` was always null, so every single reset/confirmation link silently redirected to `/login?error=confirmation_failed`. This had been true since the flow was built — S04-04 was marked Complete based on the request side's design (enumeration-safety, rate limiting) without ever actually clicking a real emailed link through to completion.

Fixed by turning `/auth/callback` into a Client Component that reads `window.location.hash` directly and calls `supabase.auth.setSession()` deterministically — not via the SDK's own `detectSessionInUrl`, which turned out to race against itself in a way that also mattered for the fix: its hash-parsing runs inside the client constructor and clears the hash as a side effect once done, so React's dev-mode double-effect-invocation (a second client instance created before the first's effect torn down) found nothing left to parse. A stable, memoized client instance plus manual hash parsing avoids that race. Verified live end to end, not just diagnosed: a real recovery link (generated the same way `resetPasswordForEmail` does) driven through an actual browser lands on `/reset-password` with a working session, sets a new password, and that new password then works for a real login; same for signup confirmation. Both are now `e2e/password-reset.spec.ts`, a permanent regression test.

**S04-04's tracker evidence needs correcting** — the Excel tracker's own row for this item should note that the original verification checked the request-side code pattern only, not a full live click-through, which is exactly how this went undetected until a real user hit it.

---

**2026-09-15 — Built out nearly every gap the Stage 6-9 audit found, across four commits**
Owner: Claude Code, per the founder's explicit "build everything missing plus the growing list of the other stages." Followed directly from the audit logged below — rather than leaving the findings as a to-do list, built real fixes for almost all of them in dependency order (Stage 6 → Stage 7/8 gaps that depend on Stage 6's verification gate → Stage 9's services flow, the largest single piece). Four commits, summarized here; each commit's own message has the full detail:

1. **Stage 6 + part of Stage 8** (`8557c87`): S06-04 verified-employer enforcement (a DB trigger, unconditional even for staff/service-role — mirrors the completeness-gate pattern already established); S06-02's org-name display bug (a new `public_organisation_names` public view, same shape as `public_talent_profiles`); S06-02/03's representative-only RLS widened to any org_admin teammate; S06-05's real team-invitation lifecycle (token, 7-day expiry, single accept — `organisation_team_invitations` + `/organisation/invite/[token]`); S06-10's "remove from shortlist"; S06-12's audit-log gaps (team role changes/removals, verification responses); S08-04's late-application blocking (RLS, not just UI); S08-05's dead confirmation banner; S08-09's rejection reason + email; S08-10/12's interview/shortlist/reapply audit gaps.
2. **Regression fixes + S09-12 + S08-14** (`d90a32c`): found and fixed two real regressions the Stage 6 batch introduced — a shared `/organisation/loading.tsx` was silently turning `notFound()`'s HTTP status from 404 into 200 for every nested route that calls it (Next.js streaming commits the response status before a nested `notFound()` throw resolves — no data leaked, but the status contract broke; fixed by scoping loading states to leaf routes that never call `notFound()` instead), and the `seedContract` e2e helper broke against S06-04's new unconditional verification gate. Also removed all real card-data collection from the simulated payment checkout (S09-12) and added `e2e/applications.spec.ts` (S08-14), previously untested at all.
3. **Stage 7 + S08-02** (`3cbe9f3`): S07-06 reopen action, S07-08 location/deadline filters, S07-10 share button, S07-11 opportunity attachments (private bucket + signed URLs), S07-15 a real browser-driven publish/discover/close e2e test; S08-02 resumable application drafts (a separate table, so a draft never touches the real `applications` uniqueness/deadline checks or becomes visible to the employer).
4. **The services flow** (`ed39b2a`): S09-03/04/05 built end to end — `service_requests` (a real request lifecycle) plus `offers`/`contracts` widened to carry an optional `service_request_id` instead of duplicating the entire delivery/payment/dispute/review machinery a second time. S09-10's live peer-pricing hint.

**Deliberately not built**, flagged instead of forced: S07-03 (legal job-notice permission workflow) depends on legal input nobody has provided — a placeholder field would be actively misleading, not a stopgap. S07-05 (two independent staff-review implementations — the older `backend/api` staff console and the newer in-app `/operations` queue) wasn't reconciled: picking one and removing the other is a real product/engineering decision with a live staff console at stake, not something to force through without the founder's input or direct UI testing access. S06-06 (recruiter/hiring_manager/finance behaving identically to member) was left as-is — it's already documented in migration `0039`'s own header as a deliberate MVP scope choice, not a bug.

Every migration was applied to staging and the specific behavior it changed was live-verified (a real unverified test org blocked from publishing, a real duplicate-application attempt rejected, a real service request walked through request → proposal → accept → contract with two live browser contexts) before moving to the next piece — the same "verify live, don't assume" discipline as every other stage this project has gone through. Full lint/typecheck/build/unit-test suites green throughout; the e2e suite was re-run in full three times to hunt for regressions, catching both real regressions in this list.

---

**2026-09-15 — Full audit of real tracker Stages 2 through 9, and the Excel tracker updated to match (68 rows, 343 cells)**
Owner: Claude Code. The founder asked for a check on whether each stage had actually been developed from Stage 2 through Stage 9 (real tracker numbering, per `AdorWorks_MVP_Development_Tracker.xlsx` / `docs/governance/stage-01-feature-inventory.md`), so the team knows where things stand and what's left to build. Found that Stage 2 (Environments/CI/test data) and Stage 3 (Data/backups/observability) had already been fully completed and documented earlier this project (`docs/governance/stage-02-environments-ci-and-test-data.md`, `docs/governance/stage-03-data-backups-observability.md`) but never transcribed into the Excel tracker; Stage 4/5/9 had already been transcribed in an earlier pass this session. That left real Stage 6 (Employer/organisation dashboard), Stage 7 (Opportunities and discovery) and Stage 8 (Applications and hiring) as the only genuinely unaudited stages — ran three parallel, evidence-based code audits (one per stage) against the actual `adorworks-fix` checkout.

Findings worth flagging specifically:
- **S06-04 (verified-employer capability boundaries) — Missing.** `organisations.verification_status` is used only for display everywhere in the codebase; nothing blocks an unverified organisation from having its opportunities approved/published, or from using self-service candidate search. The readiness-panel copy claims verification is required for publishing — that's aspirational, not enforced.
- **S06-05 (team invitations) — Missing, built differently than specced.** There's no invitation table with token/expiry/accept-reject semantics; inviting a teammate immediately creates/upserts their `organisation_members` row with no pending state to expire or reject-on-reuse.
- **S06-02 — a live display bug**: the public `/opportunities` list queries `organisations` with the RLS-gated client as a talent session, which the RLS policy doesn't allow, so real employer names silently fall back to "AdorWorks employer" for effectively every listing.
- **S07-05 — two unreconciled staff-review implementations** exist side by side (`backend/api/src/routes/opportunities.js` driving the old staff console, and `platform/src/lib/actions/operations.ts` driving a newer in-app review queue), both fully functional and audit-logged, with no sign either has been deprecated.
- **S07-03 (legal job-notice permission workflow), S07-10 (safe sharing), S07-11 (secure attachments) — Missing.** None of these three features exist at all yet.
- **S08-02 (resumable draft applications), S08-14 (end-to-end application/offer tests) — Missing.** Applications are single-step with no draft mechanism; there is zero e2e or unit test coverage anywhere for apply/withdraw/shortlist/offer flows.
- **S08-04 — a real gap**: late-application submission isn't blocked server-side at all (no RLS or action-level deadline check) — only page-level non-rendering plus a lagging (up to 1 hour) cron-driven status flip actually stop it, so a replayed request against a known opportunity ID can still insert a late application.
- Recurring theme across S06-07/S06-12/S07-15/S08-07/S08-12/S08-13: cross-organisation RLS isolation is genuinely correctly written in the current migrations, but test coverage is narrow (opportunities-table only) and migration `0070`'s own documented drift incident (see the entry below) means this class of policy has already been found silently broken in at least one environment — live re-verification in production, not just code review, is warranted before trusting it fully.

Applied all 68 rows (Stage 2: 14, Stage 3: 12, Stage 6: 13, Stage 7: 15, Stage 8: 14 — 343 cells across Status/Verification/Implementer sign-off/Evidence/Notes/Completed date) to the real Excel file at `C:\Users\kur peter\Downloads\Adorworks\AdorWorks_MVP_Development_Tracker.xlsx` using the same surgical raw-XML single-zip-entry-swap process verified safe earlier this project (never a full read/write round-trip, which destroys the file's Table objects and shared strings). Backed up the pre-update file first (`AdorWorks_MVP_Development_Tracker.backup-2026-09-15-before-stage2378.xlsx`), verified post-write that only `xl/worksheets/sheet2.xml` changed byte-for-byte from the prior version, spot-checked written values by direct cell address, and confirmed zip integrity. Founder Confirmed, Senior review, and Target/Completed-date-of-record columns were left untouched, as with every prior tracker update this project.

---

**2026-09-14 — Stage 9 re-verified live post-Netlify-outage; found and fixed a soft-404 regression and stale docs**
Owner: Claude Code. Starting Stage 9's audit, checked the live production site (`adorworks.netlify.app` per that stage's own doc) directly and found it fully down — every page returning Netlify's own `"usage_exceeded"` 503. Founder confirmed the site had already moved to Cloudflare Pages. Found the actual migration work (`build-cf-pages.mjs`'s allowlisted build output, `_headers`/`_redirects`, the domain swap to `adorworks.pages.dev` across all 20 pages) had already been done in earlier commits and was live and working — confirmed directly: homepage 200, all security headers present, `/staff/*`'s `X-Robots-Tag` present, source-exposure paths (`/docs/*`, `/backend/*`) correctly inaccessible, and — the part that actually matters for the business — the lead-capture forms' production Supabase endpoint (`intake_submissions`) live and responding, confirming form submissions aren't silently broken.

Found two things the migration had left behind: (1) `_redirects`' rules blocking source paths were rewriting to a 200-status page (Cloudflare's `_redirects` can't express a real 404 status), which is a "soft 404" — worse for SEO than doing nothing, and unnecessary since those paths already 404 correctly on their own (confirmed live) because the build step never uploads them at all. Removed the six now-redundant rules. (2) `netlify.toml` and several docs (`README.md`, `backend/api/README.md`) still described Netlify as the current host rather than retired history. Removed `netlify.toml`, updated the docs, and updated Stage 9's own governance doc to record the full migration and re-verification.

---

**2026-09-14 — RLS drift found and repaired: 12 policies across 8 migrations were tracked as applied but never actually took effect on staging**
Owner: Claude Code. Found while re-verifying Stage 8's own known gap (the employer applicant-pipeline count against real staff_assisted/self_service data — see that stage's doc). Seeding real data and reading the pipeline count *as the employer* (not the admin client) came back 3 instead of the expected 4: a self-service opportunity's `submitted` application, which migration `0046`'s `applications_select` policy is explicitly written to make visible to the employer, wasn't visible at all.

Direct inspection of the live database's actual RLS policy text (via a raw Postgres connection, `pg_policies`) confirmed `applications_select` was still word-for-word the *original* `0002` version — not `0046`'s — despite `_schema_migrations` recording `0046` (and every migration after it, up through `0069`) as applied. Wrote a script comparing every migration file's *last* intended definition of every policy (148 policy names, parsed from all 70 files) against the live database's actual text: found 12 real mismatches spanning 8 unrelated migrations (`0013`, `0015`, `0020`, `0024`, `0026`, `0039`, `0046`, `0060`) touching `talent_profiles`, `organisations`, `opportunities`, `applications`, `reviews`, `disputes`, and `finance_records`. In every case the *helper functions* these policies were supposed to start using (`is_org_member`, `is_org_write_member`, `is_contract_participant`, `has_applied_to_opportunity`, `has_invitation_for_opportunity`) already existed live and worked correctly elsewhere — only the `DROP POLICY`/`CREATE POLICY` statements themselves never took effect, in every one of these 12 cases specifically. Every case makes the live policy *more* restrictive than intended (a missing OR-branch), not less — a functionality bug (legitimate users wrongly blocked: invited team members, self-service employers, non-representative contract participants), not a security exposure.

Most likely root cause: the earlier 2026-09-14 test-project tracking-table-wipe incident's reconciliation (see that entry above) verified "the actual schema was intact through migration 0060" by spot-checking specific tables/indexes/functions — not by diffing every individual policy's exact text — and missed that these 12 policies were frozen at a pre-restore snapshot while everything else, including the newer helper functions, was already caught up.

Fixed with a new migration, `0070_rls_drift_repair.sql`, re-applying each of the 12 policies' true final text verbatim from its real source-of-truth file — plain idempotent `drop policy if exists / create policy` pairs, safe regardless of which were secretly already correct. Verified: re-ran the drift-comparison script post-fix (0 mismatches, down from 12), then re-ran the exact live Stage 8 pipeline test that surfaced this — the dashboard now correctly shows 4. Full platform test suite (59 unit + 17 e2e) and backend suite (38) all still green after the change.

**This is a staging/test-project-only finding.** Production's migration history runs through the normal CI pipeline and was never touched by the tracking-table incident this traces back to — but the founder should independently confirm production's live policy text actually matches these same 8 migration files before assuming it's unaffected, since the drift was only discoverable by direct database inspection, not by anything `_schema_migrations` or the app's own behavior would have surfaced on its own.

---

**2026-09-14 — Stage 7's known gap closed: payment idempotency and refund exercised end-to-end with real data**
Owner: Claude Code. Stage 7 (communication, notifications, contracts, payments) had already been through its own gap-check pass and had exactly one flagged item left: the payment-idempotency index and the dispute refund path were verified against the live schema but never exercised with real contract/milestone rows, since none existed in this environment at the time. Now that seeding real contracts/milestones live is routine (established during the Stage 5/6 work), ran all four scenarios for real: a payment through the actual checkout UI (correct fee/net breakdown, milestone flips to paid), a reproduction of the exact partial-failure scenario gap-check fix #1 targets (no double charge, self-heals correctly), a true concurrent double-insert against the payment_intentions unique index (exactly one wins), and a real dispute → staff refund call (payment_events flips to refunded, a finance_records row is created). All passed. See `docs/stage-7-communication-notifications-contracts-and-payments.md`'s "Known gap" section for detail. No code changes were needed — this was pure verification of already-correct code.

---

**2026-09-14 — Stage 6 gap-check pass: two real gaps found and fixed, four more flagged out of scope**
Owner: Claude Code. Stage 6 (talent introduction video, portfolio ordering, reporting granularity) was implemented before this governance process reached it and had explicitly never had the independent gap-check pass every other stage got. Ran that pass: confirmed the original implementation doc's claims against the current code and the live staging project, and found two real, live-confirmed issues — (1) the `talent-evidence` storage bucket had zero server-side size/type enforcement despite both its upload paths already claiming 8MB/jpeg/png/webp/pdf client-side (fixed, migration `0069`, verified with a real upload round-trip: wrong mime type and oversized file both now rejected, valid file still succeeds); (2) the backend's `GET /api/reports` target-type filter validation was never updated when `0056` widened the DB's allowed report target types, so filtering by the two new values would 400 (report *submission* was unaffected — that path doesn't go through this validation). Also found, while checking every storage bucket live, that four more buckets (`org-documents`, `deliverables`, `message-attachments`, `org-logos`) have the same zero-enforcement gap — deliberately NOT fixed here since, unlike `talent-evidence`, there's no already-claimed client-side limit to match, and guessing the right limits risks breaking real functionality; flagged in `docs/governance/stage-06-portfolio-video-and-media-trust.md` for whichever future stage covers organisations/contracts/messaging.

---

**2026-09-14 — S05-10 scope resolved: preferences filter/prioritize the talent's own opportunities feed; S05-04 built alongside it**
Owner: Founder (S05-10 scope), Claude Code (S05-04 build call). Asked the founder to choose between three concrete options for what "job/service preferences" should control, given no discovery-ranking or notification system existed for it to plug into; chose "filter/prioritize their opportunities feed" over "just a visible settings section" or a custom alternative. Implementation: reused `talent_profiles.category`/`.work_mode` (already captured at onboarding, not duplicated) plus one new field, `preferred_engagement_type` (migration 0068), matching the existing full_time/freelance_contract bucket the Talent Mode switcher already offers. `/opportunities` now defaults its filters from these on a genuinely untouched landing (detected by the *absence* of filter/search keys in the URL, not just falsy values — a submitted-but-blank search still counts as explicit and overrides the default), with a visible banner and an explicit `?all=1` "See everything" escape hatch so the default is never a dead end. Verified live: seeded a matching and non-matching opportunity, confirmed the bare feed shows only the match with the banner, and "See everything" reveals both. Once S05-10's scope was resolved, S05-04 (education) turned out not to need its own scoping conversation after all — its shape is conventional and directly mirrors the already-built work-experience feature (talent_education table, migration 0067, same RLS shape, EducationManager component) — built and verified live the same session per the founder's "build both" instruction.

---

**2026-09-13 — S05-13 live mobile testing caught a real pre-existing overflow bug, misdiagnosed once before being fixed**
Owner: Claude Code. A new Playwright test (`e2e/mobile-responsive.spec.ts`) checking real horizontal overflow at a 360px viewport failed on `/passport` — the *entire* authenticated app shell (header, sidebar wrapper, main content) rendered 24px wider than the viewport, not just passport-specific content, meaning every page sharing that shell was affected. First hypothesis (missing `min-w-0` on the new work-experience date-range flex row) was applied but verifiably did not fix it — re-running the identical failing test showed the identical 24px figure. Root cause, found via direct live DOM inspection against the running dev server: two native `<input type="date">` elements sitting side by side in `WorkExperienceManager` each have a ~140px floor Chromium refuses to shrink below regardless of flex/`min-w-0` overrides on their wrappers, because that floor belongs to the native control itself, not the CSS box around it — combined, the two independently forced the whole page's intrinsic minimum width past 360px. Fixed by stacking the two fields vertically below the `sm` breakpoint instead of forcing them side by side on narrow screens; verified live (all 4 tests pass, `docScrollWidth === innerWidth` exactly). Recorded because the misdiagnosis pattern (a plausible-looking flex fix that doesn't actually address a native-control floor) is a real trap worth remembering, and because it demonstrates why S05-13 needed genuine browser testing — this would not have been caught by code review alone.

---

**2026-09-14 — S05-10 (job/service preferences) deferred, not built**
Owner: Founder. Reason: the tracker item asks for preferences that "influence the intended discovery or notification flows," but no discovery-ranking or notification-preference system exists yet for a setting to actually change — matching already runs directly off onboarding's skills/category data. Building a preferences UI now would control nothing real. Impact: skipped for this pass; revisit once there's an actual discovery/notification system it would meaningfully affect. S05-04 (education) held back alongside it in Stage 5's Phase A audit, but is a separate, simpler question — proceeding with Phase B without resolving education's shape yet.

---

**2026-09-14 — Test project's migration-tracking table found wiped; reconciled, not replayed blind**
Owner: Claude Code. Reason: applying Stage 4's two new migrations (0062, 0063) to staging failed with the exact "cannot drop columns from view" conflict the project had already solved once before (`_schema_migrations` showed only 2 rows instead of 60+, so `apply-migrations.js` tried to replay the whole history from 0001). Investigated before acting: direct queries confirmed the actual schema was intact through migration 0060 (checked several specific tables/indexes/functions spanning 0050-0060, all present) — only the tracking table's bookkeeping was lost, not the real schema. Root cause unconfirmed (possibly related to the database password being reset twice in the same session investigating an unrelated connectivity issue), but this is the test project only, never production. Fixed by manually inserting tracking rows for 0001-0060 (matching confirmed reality, not blind trust) so the normal script could then correctly apply just 0061-0063 for real. Re-seeded afterward; some leftover extra rows exist from earlier testing sessions (harmless test-data cruft, not a correctness issue). Flagged to the founder and given an explicit go-ahead before touching the database directly, per the standing approach of confirming before hand-editing shared state rather than assuming.

**2026-09-13 — Stage 3 complete: S03-10 restore rehearsal verified live, all 12 steps done**
Owner: Founder / Claude Code. Reason: closing out the last open Stage 3 item. The restore-rehearsal workflow took five rounds of real, live-only-discoverable fixes before succeeding (full detail in `docs/governance/backups-and-restore.md`) — each attempt hit a genuine Supabase-specific issue no amount of code review would have caught upfront: restoring a full pg_dump collides with Supabase's own platform-managed schemas (auth/storage/extensions); even scoped to `public`, pg_dump still carries database-wide event triggers owned by a superuser Supabase never grants; `pg_restore --disable-triggers` itself needs superuser for the same reason; and this schema's own business-rule guard triggers reject already-valid data during a plain restore. Final design: data-only dumps (schema is already fully reproducible from migrations), foreign keys dropped and recreated around the load, user-defined triggers disabled and re-enabled around the load. Run #10 succeeded completely — foreign keys recreated and validated against the restored data with zero errors, real row counts confirmed. Stage 3 (data, backups and observability) is now fully complete and verified: schema/ownership map, retention policy, index review, destructive-migration policy, complete audit-event coverage, error monitoring (Sentry), uptime monitoring (UptimeRobot), structured logging, automated backups, restore rehearsal, incident-response doc, and rollback rehearsal.

**2026-09-12 — Production backup workflow live: two real bugs found by actually running it**
Owner: Founder / Claude Code. Reason: closing out S03-09. The founder added the `PROD_SUPABASE_DB_URL` secret and triggered the workflow — it failed twice, each time for a genuine reason only visible from a real run, not something code review would have caught: (1) Supabase's "Direct connection" string resolves IPv6-only, and GitHub Actions runners have no IPv6 route, so `pg_dump` failed instantly with "Network is unreachable" — fixed by switching to Supabase's **Session pooler** connection string instead (IPv4-compatible; Transaction pooler wouldn't work here since `pg_dump` needs session-level behavior transaction-mode pooling breaks). (2) Once reachable, `pg_dump` refused to run at all: Ubuntu's `postgresql-client` apt package installs v16, but Supabase's server is 17.6, and `pg_dump` hard-refuses to dump from a server newer than itself — fixed by running the job inside the official `postgres:17` Docker image instead. Third attempt (run #3) succeeded: produced a real, validated 97KB backup, confirmed via the run's artifacts API. S03-09 is now genuinely complete, not just "code merged." S03-10 (restore rehearsal) is next, now that a real backup exists to restore from.

**2026-09-12 — Correction: production has no database backup at all — S03-09 was wrong**
Owner: Claude Code / Founder. Reason: Stage 3's audit stated Supabase's free plan includes automatic daily backups. The founder tried the S03-10 restore rehearsal and hit a Pro-plan paywall, which is what surfaced the mistake — confirmed against Supabase's actual current docs: free-tier projects (both of AdorWorks's Supabase projects, including production) get no automatic backup at all; that's Pro-plan only ($25/month for the org, covering the first project, +$10/month per additional project). Impact: production had zero backup protection — a real, live gap, not the "already covered" status previously recorded. Put three options to the founder (upgrade to Pro, build a free scripted backup ourselves, or accept the risk for now) — **founder chose to build a free scheduled backup** rather than pay for Pro. Built `.github/workflows/backup-production-db.yml`: a daily `pg_dump` of production, verified with `pg_restore --list` before upload, kept as a GitHub Actions artifact for 30 days. Needs one founder-side step before it's live: adding a `PROD_SUPABASE_DB_URL` repository secret directly in GitHub (deliberately never asked for in chat, since it's a full-access production database credential unlike anything else handled this way so far). Full detail and restore instructions: `docs/governance/backups-and-restore.md`.

**2026-09-12 — Stage 3 founder decisions: retention policy, error monitoring, uptime monitoring (S03-02, S03-06, S03-07)**
Owner: Founder. Reason: Stage 3's audit flagged three items needing a founder call before proceeding (`docs/governance/stage-03-data-backups-observability.md`). Decided: (1) data retention — keep indefinitely for now, deletion is staff-assisted only, no self-service delete flow yet (`docs/governance/data-retention-policy.md`); (2) error monitoring — proceed with Sentry's free tier; (3) uptime monitoring — proceed with UptimeRobot's free tier. Impact: (1) is fully documented and in effect immediately, no code change needed. (2) and (3) need the founder to actually create the vendor accounts (email signup, no payment) before the wiring/configuration work can happen — flagged as the next founder-side step, not yet done.

**2026-09-12 — Branch protection recreated on `main` with owner bypass (S02-12) — Stage 2 fully complete**
Owner: Founder. Reason: re-add the protection removed in Stage 1, without disrupting the founder+Claude Code direct-push workflow discussed beforehand. Impact: `main` is protected again; a push without bypass rights would now be blocked. Verified live immediately: the next push after this was recreated returned the same `Bypassed rule violations for refs/heads/main: Cannot update this protected ref.` message seen before the ruleset was deleted, confirming the rule is genuinely active and the bypass genuinely works — nothing about how this project is built day to day changes. All 14 Stage 2 steps are now complete.

**2026-09-12 — Staging environment fully live: Stage 2 done except S02-12**
Owner: Founder + Claude Code. Reason: completed the Render half of staging (Vercel's half landed earlier the same day). Impact: a dedicated `staging` git branch now exists so the platform app's staging URL is permanent (`https://ador-works-git-staging-kurpeter20000s-projects.vercel.app`) rather than changing per branch; a second free Render service (`adorworks-api-staging`, `https://adorworks-api-staging.onrender.com`) runs backend/api against the test database. Verified live, not just deployed: created a temporary staff account directly in the test database, signed in for a real token, and called the staging API's `GET /api/organisations` — got back the exact seeded organisation ("Nile Youth Foundation"), proving genuine database connectivity through real authentication. Temporary account deleted immediately after. All 14 Stage 2 steps are now addressed; only S02-12 (branch protection) remains, and it's a founder decision, not blocked on any further work.

**2026-09-12 — Vercel Preview environment now points at the test database, verified live (S02-01/S02-05)**
Owner: Founder + Claude Code. Reason: needed a real staging deployment of the platform app, not just a database. Impact: production's `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY` and `SUPABASE_SECRET_KEY` had been saved as Vercel "Secret" type since before this project (Aug 24) — turned out that also locks the environment scope, not just the type, so narrowing them to Production-only required deleting and recreating each (production's real values recovered from the Supabase dashboard first, never from Vercel, since Secret values can't be read back). Separate Preview-scoped entries were then added pointing at the test project. Verified for real: pushed a disposable branch, waited for the resulting Vercel preview deployment, and logged into it with a seeded test account — it showed the exact seeded data (talent "Amara Deng," the seeded opportunity), proving the preview genuinely reads the test database rather than production.

**2026-09-12 — Staging reuses the test Supabase project, not a third one**
Owner: Founder. Reason: Supabase's free plan only allows 2 active projects per organization; production + the test project already use both. A third project would need pausing another one manually, or a paid plan upgrade. Impact: staging and the automated e2e test database are the same Supabase project. Trade-off accepted: don't run the e2e suite at the same time someone's manually reviewing staging, since the suite creates/deletes real records there.

**2026-09-12 — Corrected the migration-automation approach after finding it broken live**
Owner: Claude Code. Reason: the first version of `apply-migrations.js` re-ran every migration file unconditionally on every invocation, reasoning that each file's own DDL is individually safe to re-apply. That's true per-file but not true of the sequence as a whole — confirmed live: migration 0003 defines a view that 0034 later redefines with a different column set, and Postgres's `CREATE OR REPLACE VIEW` can't drop existing columns, so replaying 0003 after 0034 has run fails outright. Impact: rebuilt using a `_schema_migrations` tracking table so a file is only ever applied once, which is the standard/correct approach and doesn't have this failure mode. Verified both code paths (nothing pending, and exactly-one-thing pending) directly against the live test project before considering this done.

**2026-09-12 — First dedicated test Supabase project created (S02-07)**
Owner: Founder + Claude Code. Reason: needed to actually verify the seed script and run the e2e suite for real instead of leaving them "looks right, untested." Impact: a real, separate Supabase project (`adorworks-test`) now exists, all 60 migrations applied cleanly to it. Running the seed script against it for real found two genuine bugs (a bad `onConflict` column, and an old `budget_min`/`budget_max` field name that the current schema replaced with `compensation_*`) — both fixed. The existing end-to-end test suite also ran successfully for the first time in this project's history: 11/12 passed, 1 flaky on a heavier test (passed on retry, looks like timing rather than a real bug). Credentials live in `platform/.env.e2e.local`, git-ignored, never committed.

**2026-09-12 — Pilot geography: South Sudan only. Languages: English, Swahili, Arabic**
Owner: Founder. Reason: founder decision (S01-02). Impact: the pilot does not target other countries in the region yet — that's a deliberate later expansion, not MVP scope. The product needs to support three languages; it is currently English-only everywhere (marketing site, platform app, staff console). This is a substantial, cross-cutting body of work with no dedicated tracker stage of its own — closest fit is S12-15 ("Standardise interface language and content"), which as written doesn't fully capture a three-language requirement. Flagging now so it's designed for from here on rather than retrofitted later.

**2026-09-12 — Founder deleted the existing branch-protection ruleset on `main`**
Owner: Founder. Reason: founder's own action, taken directly in GitHub after learning (from the prior correction above) that a rule existed and every push was going through as a bypass. Impact: `main` now has no branch protection at all — direct pushes go through cleanly with no bypass logging. Combined with the standing commit/push authorization, this means there is currently no independent safety net between a mistake and it being live on `main`. Revisit under S01-07/S02-12 once the team grows beyond just the founder and Claude Code, or sooner if the founder wants it back.

**2026-09-12 — Correction: `main` does have some branch protection, discovered live**
Owner: Claude Code. Reason: the Stage 1 write-up initially said branch protection status was unverifiable and assumed it wasn't set up. The very next push proved that wrong: GitHub returned `Bypassed rule violations for refs/heads/main: Cannot update this protected ref.` — a rule exists, and the push succeeded only because it was logged as an admin bypass. Impact: corrected both Stage 1 documents rather than leaving the wrong assumption on record. Still open: what the rule actually requires, and whether the founder wants to keep it bypassable for direct pushes or tighten it now that the gap is known.

**2026-09-12 — No separate Engineer A / Engineer B for now; Claude Code covers both roles**
Owner: Founder. Reason: no other engineers are on the project yet. Impact: until real engineers join, I (Claude Code) do the work the playbook assigns to both Engineer A and Engineer B, under the founder's review — legal counsel, security reviewer and pilot team roles remain unfilled and genuinely blocked, since the playbook is explicit those cannot be substituted.

**2026-09-12 — Standing authorization to commit and push every change**
Owner: Founder. Reason: the founder doesn't code and wants me to build directly rather than asking for commit-by-commit permission for routine work. Impact: I commit and push without asking each time; the founder's direct approval is reserved for anything sensitive or potentially costly (security, legal, payments, production-affecting, or otherwise high-stakes), which I still flag and wait on. `main` currently has no branch protection (S01-07 still open), so this relies on care rather than a safety net catching mistakes before they're live — noted as an accepted tradeoff for now.

**2026-09-12 — Adopted the Claude Code Development Playbook as the governance process**
Owner: Founder. Reason: prior work in this project happened ad hoc across several parallel sessions without a shared process, stage sequencing, or a single tracker of what's actually done vs. assumed done. Impact: all further work follows the playbook's stage/step-ID/Definition-of-Done model; the Excel tracker is now the source of truth for scope and status.

**2026-09-11 — Migrated the public marketing site from Netlify to Cloudflare Pages**
Owner: Founder + Claude Code. Reason: `adorworks.netlify.app` was paused by Netlify itself for hitting free-tier usage limits, breaking every "back to the website" link from the platform app in production. Impact: canonical/OG/sitemap references, and every link inside the platform app, now point at `adorworks.pages.dev`. A real build step (`build-cf-pages.mjs`) was added — a first for this repo, which previously had no build step for the static site — because relying on `_redirects` alone to block `/backend/`, `/platform/`, `/docs/`, `/.github/` from being publicly servable turned out not to work (Cloudflare Pages' `_redirects` silently rejects a `404` status code, confirmed from the actual deploy log after an earlier, wrong diagnosis blamed a different cause).

**2026-09-11 — Kept Cloudflare Pages rather than switching to Workers Static Assets**
Owner: Claude Code, with founder's directional question. Reason: Cloudflare is steering new projects toward Workers Static Assets, but it has no documented mechanism to exclude specific files/directories from deployment, and its `_redirects` explicitly does not override a real uploaded file — the opposite of what this site needs to keep `backend/`/`platform/`/`docs/` from being publicly exposed. Pages' behavior does. Impact: this project's static site stays on Pages; revisit only if the site itself needs real server-side/edge logic later.

**2026-09-11 — Closed several audit-logging gaps in the backend API**
Owner: Claude Code (backend session). Reason: a prior internal security review (`docs/stage-10-...md`, defect #5, High severity) found dispute resolution, refund issuance, manual finance-record changes, opportunity moderation, and onboarding-agent role grants were never audit-logged, despite the event names already existing in the shared vocabulary (`platform/src/lib/domain/events.ts`) — the wiring was just never done. Impact: those five actions now write to `audit_events`; corresponding new event-name constants were added to keep the platform and backend vocabularies in sync.

**2026-09-10 — Redesigned the platform app's shell: persistent sidebar + mode switcher**
Owner: Founder + Claude Code. Reason: the previous top-nav-only layout didn't scale to a growing route list, and there was no clean way for a user to move between "using the app" and "browsing the public site," or (for talent) between full-time and freelance/contract work-type filtering. Impact: `platform/` now has a left sidebar (desktop) / drawer (mobile) and a top-bar mode switcher (Explore Mode / Client Mode / Talent Mode with Full-time / Freelancing & Contract sub-filters). Superseded the old `top-nav.tsx`/`top-nav-client.tsx` components entirely.

**2026-09-09 — Redesigned the login/signup pages with a full-bleed hero video background**
Owner: Founder + Claude Code. Reason: the original login page was judged too plain and too narrow for a laptop-width screen. Impact: reused the marketing site's existing hero video (no new asset sourced), added an "Explore AdorWorks" affordance consistent with the later mode-switcher naming, and widened the card for larger screens.
