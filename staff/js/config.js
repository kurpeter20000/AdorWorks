/*
  Same Supabase project as the public site — this is the "publishable"
  key, safe to be public (Row Level Security is the actual guard).
  API_BASE_URL is the deployed backend/api service (see
  ../../backend/api/README.md) — fill it in once that's deployed;
  until then, every write action on these pages will show a clear
  "API not configured" error instead of failing silently.
*/
window.ADORWORKS_SUPABASE_URL = "https://cpiebggzbxshzvlzqdfn.supabase.co";
window.ADORWORKS_SUPABASE_ANON_KEY = "sb_publishable_7DGYFu8GzBwM0WwcDnEmnQ_Ha73j8hq";
window.ADORWORKS_API_BASE_URL = "";
